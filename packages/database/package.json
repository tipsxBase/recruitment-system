{
  "name": "@recruitment/database",
  "version": "1.0.0",
  "description": "Shared database client for recruitment system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "dev": "tsc --watch",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1",
    "@types/node": "^20.3.1"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
